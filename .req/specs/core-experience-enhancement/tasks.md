# 任務清單：核心體驗增強

## 對應規格
- Spec: .req/specs/core-experience-enhancement/spec.md
- Plan: .req/specs/core-experience-enhancement/plan.md

---

## Phase E：移除 mock（清理基礎）

### 任務 E.1：後端移除 SimulatedCrawler
- **對應 User Story**：US-17
- **描述**：從 CrawlerRouter 移除 SimulatedCrawler，只保留真實爬蟲。搜尋 API 無結果時回傳空列表。
- **驗收條件**：
  - [ ] CrawlerRouter 不再註冊 SimulatedCrawler
  - [ ] 外站票搜尋的 SimulatedCrawler 也移除
  - [ ] API 回傳結果中不再有 source: "simulated"
  - [ ] 無結果時回傳 { results: [], result_count: 0 }
- **測試策略**：整合測試 — 驗證無 simulated source
- **狀態**：`todo`

### 任務 E.2：前端移除 mock fallback + 錯誤處理
- **對應 User Story**：US-17
- **描述**：移除前端 mock.ts 的所有引用，API 失敗時顯示錯誤訊息 + 重試按鈕
- **驗收條件**：
  - [ ] 前端不再 import mock.ts
  - [ ] API 失敗時顯示「查詢失敗，請稍後再試」+ 🔄 重試按鈕
  - [ ] 無結果時顯示「暫無航班資料」+ 建議「加入追蹤，每天自動查詢」
  - [ ] 三種搜尋模式都有正確的錯誤處理
- **測試策略**：手動測試 — 斷網/錯誤情境
- **狀態**：`todo`

---

## Phase F：行程規劃 + 記帳前端

### 任務 F.1：行程規劃前端頁面 [P-group-F]
- **對應 User Story**：US-18
- **描述**：實作完整的行程規劃前端，接上已有的後端 API
- **驗收條件**：
  - [ ] 「我的旅程」列表：顯示所有計畫（卡片式）
  - [ ] 建立新計畫表單：名稱、目的地、日期、預算
  - [ ] 計畫詳情頁：按天顯示行程項目
  - [ ] 新增/編輯/刪除行程項目
  - [ ] 邀請旅伴：產生連結 + 複製按鈕
  - [ ] 定案流程：發起 → 確認狀態 → 鎖定
  - [ ] 成員列表 + 角色顯示
- **測試策略**：手動測試 + 建置驗證
- **狀態**：`todo`

### 任務 F.2：記帳拆帳前端頁面 [P-group-F]
- **對應 User Story**：US-19
- **描述**：實作完整的記帳拆帳前端，接上已有的後端 API
- **驗收條件**：
  - [ ] 快速記帳：大字金額輸入 + 付款人 + 類別（2 秒內完成）
  - [ ] 花費列表：按時間排序，金額 + 付款人 + 類別圖示
  - [ ] 預算進度條：已花 / 預算，超支變紅
  - [ ] 花費分佈橫條圖（交通/住宿/餐飲/門票/其他）
  - [ ] 拆帳結算：誰付誰多少 + 已結清標記
  - [ ] 結算結果一鍵複製文字
- **測試策略**：手動測試 + 建置驗證
- **狀態**：`todo`

---

## Phase G：機票 → 行程整合

### 任務 G.1：航班卡片「加入行程」功能
- **對應 User Story**：US-20
- **描述**：搜尋結果的航班卡片加上「+ 加入行程」按鈕，點擊後選擇計畫並自動建立行程項目
- **驗收條件**：
  - [ ] 單程/來回結果卡片顯示「+ 加入行程」按鈕
  - [ ] 外站票結果卡片也支援
  - [ ] 點擊彈出計畫選擇下拉（含「建立新計畫」選項）
  - [ ] 選擇後呼叫 API 建立行程項目（type=transport, name=航空+航班號, cost=票價）
  - [ ] 外站票一次加入 4 段航程
  - [ ] 成功提示「已加入 [計畫名稱]」
- **測試策略**：手動測試 — 加入流程
- **狀態**：`todo`

### 任務 G.2：後端支援從航班建立行程項目
- **對應 User Story**：US-20
- **描述**：新增 API 端點或擴展現有端點，支援從航班資訊快速建立行程項目
- **驗收條件**：
  - [ ] POST `/api/trips/{id}/items/from-flight` 接受航班資訊，自動填入行程項目
  - [ ] 支援批次加入（外站票 4 段）
- **測試策略**：整合測試
- **狀態**：`todo`

---

## Phase H：票價監控與提醒

### 任務 H.1：票價歷史 + 目標價格後端
- **對應 User Story**：US-21
- **描述**：新增 price_history table，crawl_schedules 加 target_price，每日爬取後比對
- **驗收條件**：
  - [ ] price_history 記錄每次爬取的最低價
  - [ ] crawl_schedules 支援 target_price 欄位
  - [ ] PUT `/api/crawl-schedules/{id}/target` 設定目標價格
  - [ ] 每日爬取後比對：低於目標 → alert_triggered = true
  - [ ] GET `/api/crawl-schedules` 回傳 alert_triggered 狀態
- **測試策略**：整合測試 — 價格比對邏輯
- **狀態**：`todo`

### 任務 H.2：票價提醒前端
- **對應 User Story**：US-21
- **描述**：追蹤頁面加入目標價格設定，搜尋結果加入歷史新低標籤
- **驗收條件**：
  - [ ] 追蹤列表每筆顯示：當前最低價、目標價、差距百分比
  - [ ] 可設定/修改目標價格（輸入框 + 儲存）
  - [ ] 低於目標的航線置頂 + 🔔 醒目標示
  - [ ] 搜尋結果如果為歷史新低顯示「🏷️ 歷史新低！」
- **測試策略**：手動測試
- **狀態**：`todo`

---

## 進度摘要
- 總任務數：8
- 已完成：0
- 進行中：0

## 依賴關係

```
E.1 ──→ E.2（mock 後端先移除，前端才改）
F.1 ┐
F.2 ┤──→ G.1（行程前端做好才能「加入行程」）
    │    G.2
    └──→ H.2（記帳做好才完整）
H.1 ──→ H.2
```
